
Installation:

    $ curl https://sh.rustup.rs -sSf | sh

Cargo tool:

    $ cargo build
    $ cargo run
    $ cargo new hello_world --bin

Hello world:

    fn main() {
        println!("Hello, world!");
    }

Conventions:
    Filenames contain "_"
